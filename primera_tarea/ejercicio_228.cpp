/*(dígitos de un número entero) escriba un programa que ingrese un número entero de cuatro dígitos,
separe el número entero en sus dígitos y los imprima en orden inverso separados por dos espacios cach. 
 por ejemplo, si el usuario escribe 4315, el programa debería imprimir.*/

#include <iostream>

using namespace std;

int main() {
    int num, a1, a2, a3, a4;

    cout << "Ingrese un numero numeros enteros seguidos sin espacio: ";
    cin >> num;


    cout << "invirtiendo los numeros: " << a4 << "  " << a3 << "  " << a2 << "  " << a1 << endl;
    
    return 0;
}